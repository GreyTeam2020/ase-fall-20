import calculator as util

class FooCalculatorClass:

    def __init__(self):
        pass

    def sum(self, elem_one, elem_two):
        return util.sum(elem_one, elem_two)

    def div(self, elem_one, elem_two):
        return util.divide(elem_one, elem_two)